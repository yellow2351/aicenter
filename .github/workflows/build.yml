name: Build APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
    
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
    
    - name: Install Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
    
    - name: Install npm dependencies
      run: npm install
    
    - name: Download runtime
      run: |
        chmod +x scripts/download-runtime.sh
        ./scripts/download-runtime.sh
    
    - name: Prepare Android assets
      run: |
        chmod +x scripts/prepare-android-assets.sh
        ./scripts/prepare-android-assets.sh
    
    - name: Build APK with Gradle
      run: |
        cd android
        gradle assembleDebug --no-daemon
    
    - name: Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: ai-agent-control-center-apk
        path: android/app/build/outputs/apk/debug/app-debug.apk
    
    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        files: android/app/build/outputs/apk/debug/app-debug.apk
        tag_name: v1.0.0
        name: AI Agent 中控台 v1.0.0
        body: |
          AI Agent 中控台 Android 应用
          
          功能特性：
          - 完全独立运行，无需预装 Termux
          - 后台静默运行，用户无感知
          - 支持 SillyTavern/AstrBot/Ollama/Kobold AI
          - Neumorphism 设计风格
          - WebSocket 实时状态监控
          
          安装说明：
          1. 下载 APK 文件
          2. 在 Android 设备上安装
          3. 首次启动会自动初始化环境（约 10-20 秒）
          4. 开始使用
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
